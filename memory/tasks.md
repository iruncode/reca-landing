# Tâches — reca-landing

- [x] Construire la landing page (React + Vite), toutes les sections. — Commit `058ac76`.
- [x] Corriger l'erreur de build Vercel ("No Output Directory named build"). — Ajout de `vercel.json` (`outputDirectory: dist`), commit `530684f`, poussé sur `origin/main`. Confirmé fonctionnel par l'utilisateur le 2026-07-19 : le déploiement Vercel marche.
- [x] Mettre en place le système de mémoire du projet (`memory/`). — Adapté depuis un `CLAUDE.md` d'un autre projet fourni par l'utilisateur (partie modules Contrats/Mapbox/Supabase et bloc RTK écartés car non pertinents/non installés ici).
- [ ] Migrer `netlify/functions/contact.js` vers le format Vercel (`/api`) — nécessaire seulement si Vercel devient la cible de déploiement réelle du formulaire de contact. Explicitement reporté par l'utilisateur le 2026-07-19.
- [ ] Vérifier un domaine d'expédition réel dans Resend et mettre à jour `FROM_EMAIL` dans `netlify/functions/contact.js`.
- [x] Refonte "landing-v2-optimisation" (branche du même nom) : nouvelles sections `TrustBar`, `Problems`, `Solutions` (remplace `WhyReca`), `Included`, `TaxCredit`, `Faq`, `MobileStickyBar`, `SocialProof` (remplace `Testimonials`), et `ContactForm` transformé en formulaire à 3 étapes (wizard). Tous les composants + `App.tsx` + la fonction Netlify avaient déjà été écrits, mais **aucun CSS n'avait été ajouté** pour ces nouveaux composants — ils s'affichaient sans style. CSS manquant ajouté dans `src/styles/global.css` (trust bar, cartes problèmes/solutions numérotées, layout "inclus" + garanties, section crédit d'impôt, accordéon FAQ, barre sticky mobile, styles du wizard). CSS mort de l'ancien design (`.why-*`, `.testi-*`) supprimé. Commité (`960e6d1`) et poussé sur `origin/landing-v2-optimisation` le 2026-07-25.
- [x] Branche `optimisation-mobile` (créée depuis `landing-v2-optimisation` le 2026-07-25) : Chromium a finalement pu être installé pour Playwright dans ce sandbox (le blocage précédent était un téléchargement lent d'environ 300 Mo, pas un réseau bloqué — la prochaine fois, laisser tourner plusieurs minutes en surveillant la taille du fichier avant de conclure à un blocage). Vérification visuelle réelle desktop (1280px) + mobile (375px) + captures à 320/768/900px, clic à travers tout le wizard de contact, ouverture du menu mobile, accordéon FAQ, navigation par ancre. Bugs réels trouvés et corrigés dans `global.css` :
  - Le CTA "Soumission gratuite" de la nav passait sur 2 lignes sous ~700px (nav trop haute) → masqué sous 700px car `MobileStickyBar` fournit déjà ce CTA en permanence.
  - Aucun `scroll-margin-top` sur les sections : cliquer un lien d'ancre (nav, footer) faisait atterrir le titre de section caché sous la nav sticky → ajouté `scroll-margin-top: 88px` sur `section[id]`.
  - Bouton "Retour" du wizard de contact (étapes 2 et 3) qui devenait un cercle illisible sous 480px (les `flex:1` avec `min-width:auto` laissaient le long texte "Recevoir ma soumission gratuite" écraser l'espace) → boutons empilés pleine largeur sous 480px, CTA principal en premier.
  - Léger dépassement horizontal sub-pixel à 320px (négligeable, un `<span>` de neige) → `overflow-x: hidden` ajouté sur `html` par précaution.
  - Aucune erreur console sur aucun des deux viewports. Build (`npm run build`) passe.
- [x] Restructuration de la page demandée par l'utilisateur (2026-07-26, branche `optimisation-mobile`) : Hero laissé intact ; `Problems`+`Solutions` fusionnés en `ProblemsSolutions.tsx` (4 paires côte à côte) ; `HowItWorks`, `Included`, `SocialProof`, `Faq` supprimés ; `TaxCredit` (admissibilité crédit d'impôt) et `Zones` (territoire) remontés juste après le Hero/TrustBar ; nouveau `SubmissionTypeModal.tsx` (popup Particulier/Commercial) déclenché par le CTA du Hero, préremplit `typeEntree` du wizard de contact pour "Commercial" ; `ContactForm` perd son bloc "Dernière étape" mais garde le formulaire et les coordonnées ; `Nav`/`Footer` mis à jour pour ne plus pointer vers des ancres supprimées. Vérifié avec `npm run build` (passe) et Playwright (aucune erreur console, flux modal→formulaire→préremplissage testé desktop + mobile, captures d'écran conformes).
- [x] Popup de soumission avec formulaire complet (2026-07-26, branche `optimisation-mobile`) : suite au retour utilisateur que l'ancien popup (2 boutons puis scroll) faisait perdre le contexte Particulier/Commercial, le wizard de `ContactForm` a été extrait dans `ContactWizard.tsx` (réutilisable, prop `idPrefix`), une étape 0 "Particulier ou Commercial ?" ajoutée (4 étapes au total), et `SubmissionTypeModal.tsx` remplacé par `SubmissionModal.tsx` qui affiche tout le wizard dans un popup qui s'ouvre sur place (aucun scroll). Tous les boutons "Soumission gratuite" du site (Nav, Hero, `MobileStickyBar`) ouvrent maintenant ce popup ; la section de bas de page reste disponible en parallèle (son propre `ContactWizard` indépendant). Vérifié avec `npm run build` (passe) et Playwright : aucun scroll au clic, aucun id HTML dupliqué entre les deux instances du wizard montées simultanément, préselection "Multilogement ou commercial" correcte, fermeture Escape fonctionnelle, boutons Nav/sticky mobile testés, captures desktop + mobile conformes.
- [x] Série de retouches visuelles demandées par l'utilisateur (2026-07-26, branche `optimisation-mobile`), toutes dans `src/styles/global.css` sauf mention contraire :
  - Cartes de `ProblemsSolutions` (`.problem-solution-pair`) agrandies : une carte pleine largeur par ligne (`.ps-grid` passé à 1 colonne), problème/solution côte à côte avec flèche pivotée à l'horizontale sur desktop, texte et icônes agrandis (`h3` 1.35rem, `p` 1.08rem, icônes 56px).
  - Effet hover "grossit + glow" ajouté sur tous les boutons du site (`.btn`, `.sticky-btn`, `.wizard-type-choice`) : `transform: scale()` au survol (fait grossir le bouton et son texte ensemble) + `box-shadow` coloré, retour à l'état normal hors survol via les règles `:hover` standards (aucun JS).
  - `src/components/Nav.tsx` : ajout du lien "Comment ça marche?" en premier dans `LINKS`, pointant vers `#problemes-solutions`.
  - Texte agrandi dans `TaxCredit` (`.tax-credit-layout h2`, paragraphes, `.tax-credit-example`, `.tax-credit-math`, `.tax-credit-label`).
  - Cartes de `Services` (`.service-card`) agrandies : padding, icônes (64px), `h3` (1.5rem), paragraphe et items de liste plus grands.
  - **Bug mobile trouvé et corrigé** : agrandir `.tax-credit-math` a fait déborder la page horizontalement sous 480px (jusqu'à 59px à 320px) car un track de grille `1fr` seul hérite quand même de la largeur minimale de son contenu (même piège que le `min-width:auto` des flex items déjà rencontré sur ce projet) — corrigé avec `grid-template-columns: minmax(0, 1fr)` sous 900px + `.tax-credit-math` qui rétrécit/wrap sous 480px. Les deux correctifs sont dans des media queries mobiles, aucun impact sur l'apparence desktop (vérifié par capture d'écran comparative).
  - Audit Playwright complet (320/375/414px, plein écran + captures ciblées + mesure `scrollWidth` vs `clientWidth`) : plus aucun débordement horizontal après correctif, aucune erreur console.
